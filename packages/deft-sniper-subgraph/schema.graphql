type Swap @entity {
  id: ID!
  txHash: Bytes!
  sender: Bytes!
  amountOutMin: BigInt!
  path: [Bytes!]
  deadline: BigInt!
  isBot: Boolean!

  txHash: Bytes!

  logIndex: BigInt!
  blockNumber: BigInt!
  timestamp: BigInt!
}

type Global @entity {
  id: ID!
  botsDetected: BigInt!
}
